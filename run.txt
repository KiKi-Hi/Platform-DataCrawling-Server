# 1. Redis 실행 (docker 등)
redis-server

# 2. Celery 워커 실행
celery -A celery_worker.celery_app worker --loglevel=info

# 3. FastAPI 실행
uvicorn main:app --host 0.0.0.0 --port 8001 # main.py안의 app FastAPI 애플리케이션 인스턴스 실행
uvicorn main:app --host 0.0.0.0 --port 8001 --reload # 자동 재시작 기능
